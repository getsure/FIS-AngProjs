import { MycustcolorDirective } from './mycustcolor.directive';

describe('MycustcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new MycustcolorDirective();
    expect(directive).toBeTruthy();
  });
});
