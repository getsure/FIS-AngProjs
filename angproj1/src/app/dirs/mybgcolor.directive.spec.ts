import { MybgcolorDirective } from './mybgcolor.directive';

describe('MybgcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new MybgcolorDirective();
    expect(directive).toBeTruthy();
  });
});
